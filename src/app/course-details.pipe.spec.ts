import { CourseDetailsPipe } from './course-details.pipe';

describe('CourseDetailsPipe', () => {
  it('create an instance', () => {
    const pipe = new CourseDetailsPipe();
    expect(pipe).toBeTruthy();
  });
});
